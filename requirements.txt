# ── API framework ──────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0

# ── Rate limiting ──────────────────────────────────────────────────────────────
# slowapi provides per-IP rate limiting on FastAPI endpoints.
slowapi>=0.1.9

# ── API framework ──────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0

# ── Rate limiting ──────────────────────────────────────────────────────────────
slowapi>=0.1.9

# ── ML / Data (pinned — must match the versions the model pickles were saved with) ──
scikit-learn==1.8.0
numpy==2.3.5
scipy==1.17.1
joblib>=1.3.2
shap>=0.45.0
pandas>=2.2.0

# ── Notebook / EDA ────────────────────────────────────────────────────────────
openpyxl>=3.1.0
matplotlib>=3.8.0
seaborn>=0.13.0

# ── LLM (recommendations engine) ──────────────────────────────────────────────
google-genai>=1.0.0

# ── Notebook / EDA ────────────────────────────────────────────────────────────
openpyxl>=3.1.0
matplotlib>=3.8.0
seaborn>=0.13.0

# ── LLM (recommendations engine) ──────────────────────────────────────────────
# Google Gen AI SDK — used by /generate/recommendations to synthesise risk-
# reduction guidance. The endpoint falls back to a deterministic substrate if
# no GEMINI_API_KEY is set, so this is required only to enable LLM-phrased output.
google-genai>=1.0.0
